name: Release Mobile APK

on:
  push:
    tags: ["v*"]

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable

      - name: Build APK
        working-directory: mobile
        run: |
          if [ -n "${{ secrets.GOOGLE_CLIENT_ID }}" ]; then
            flutter build apk --release --dart-define=GOOGLE_CLIENT_ID=${{ secrets.GOOGLE_CLIENT_ID }}
          else
            flutter build apk --release
          fi

      - name: Rename artifact with tag name
        run: |
          cp mobile/build/app/outputs/flutter-apk/app-release.apk \
             DaySkew-${{ github.ref_name }}.apk

      - name: Publish GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          name: DaySkew ${{ github.ref_name }}
          body: |
            ## DaySkew Mobile App

            Android APK for release ${{ github.ref_name }}.

            - Constraint-based reflow scheduler (wake-up hero, timeline, conflict resolution)
            - Points to the live backend at https://dayskew.danailmihov.com/api
            - Apple Human Interface Guidelines design: adaptive light/dark, system colors, iOS patterns

            Download `DaySkew-${{ github.ref_name }}.apk` and install on any Android device.
          files: DaySkew-${{ github.ref_name }}.apk
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}